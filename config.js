// config.js
module.exports = {
  // WhatsApp phone number with country code, no "+" sign
  phoneNumber: '233266309343',

  // Optional: set true if you want to print QR (not needed for 6-digit flow)
  printQR: false,
};
